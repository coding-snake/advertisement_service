{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.task_delete'|trans({'%id%': task.id|default('')}) }}
{% endblock %}

{% block body %}
    {% include 'default/_form.html.twig' with {
        page_title: 'title.task_delete'|trans({'%id%': category.id|default('')}),
        addit: task.title,
        form: form,
        submit_label: 'action.delete'|trans,
        back_to_list_path: 'category_index',
    } %}
{% endblock %}