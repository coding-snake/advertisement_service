
Bazy danych:
docker-compose up -d
docker-compose exec php bash
cd app
bin/console doctrine:migrations:migrate
bin/console doctrine:fixtures:load
yes
yes


Narzędzia do poprawy kodu:

vendor/bin/php-cs-fixer fix src/ --rules=@Symfony,@PSR1,@PSR2,@PSR12

vendor/bin/phpcs --standard=Symfony src/ --ignore=Kernel.php

vendor/bin/phpcbf --standard=Symfony src/ --ignore=Kernel.php

---

Serwis ogłoszeniowy
    dostateczny     (2/6)
        📌 użytkownicy: administrator mający możliwość tworzenia, edycji i usuwania treści na stronie oraz użytkownik niezalogowany, mający możliwość anonimowego dodawania ogłoszeń,
            ✅ Administrator o danych logowania: admin@example.com i haśle admin1234
            ✅ Użytkownik niezalogowany działa poprzez podpinanie wszystkich anonimowych ogłoszeń do jednego użytkownika anonimowego o danych logowania: anon@example.com i haśle anon1234
        📌 CRUD dla ogłoszeń (użytkownik niezalogowany ma możliwość tworzenia nowych ogłoszeń, o ich aktywacji i pojawieniu się na stronie decyduje administrator),
            ✅ Nowe ogłoszenia stworzone przez użytkowników niezalogowanych dostępne są pod adresem /task/task, lista ta dostępna jest tylko dla administratora
            ✅ Ogłoszenia zaakceptowane przez administratora dostępne są pod adresem /task/accepted
            ✅ (C) Akcja tworzenia nowego ogłoszenia dostępna jest pod tytułem listy ogłoszeń (zarówno zaakceptowanych przez administratora jak i nie), lub /task/create
            ✅ (R) Akcja czytania danego postu możliwa jest poprzez kliknięcie linku koło danego ogłoszenia, bądź udanie się do /task/{id}
            ✅ (U) Akcja dostępna jest poprzez odpowiedni link (nie wyświetlajacy się jeżeli nie jesteśmy zalogowani) w /task/{id} dla danego ogłoszenia
            ✅ (D) Akcja usunięcia możliwa jest poprzez odpowiedni link (nie wyświetlający się jeżeli nie jestesmy zalogowani) w /task/{id}/delete
            ✅ Administrator ma możliwość aktywacji danego postu opublikowanego przez użytkownika anonimowego jeżeli na /task/{id} danego ogłoszenia kliknie on w link "zaakceptuj"

        📌 CRUD dla kategorii, łączenie kategorii z ogłoszeniami (użytkownik niezalogowany ma wyłącznie możliwość dodania ogłoszenia do istniejących kategorii),
            ✅ Kategorie połączone są z ogłoszeniami za pomocą pola relacji, podczas tworzenia ogłoszenia dodajemy je do danej kategorii
            ✅ (C) Akcja tworzenia nowej kategorii dostępna jest tylko dla użytkowników zalogowanych, poprzez link umieszczony na liście kategorii /category lub bezpośrednio przez /category/create
            ✅ (R) Akcja ta pozwala nam na wyświetlenie listy postów dla danej kategorii, można aktywować ją poprzez kliknięcie odpowiedniej akcji na liście /category, bądź bezpośrednio przez /task/category/{id}
            ✅ (U) Akcja ta pozwala nam na zmienienie nazwy danej kategorii, można wywołać ją przez kliknięcie odpowiedniej akcji na /category, lub bezpośrednio przez link /category/{id}/edit
            ✅ (D) Kategoria może zostac usunięta przez kliknięcie odpowiedniej akcji na /category, bądź bezpośrednio przez link /category/{id}/delete

        📌 Wyświetlanie listy ogłoszeń dla danej kategorii
            ✅ Możliwe przez kliknięcie odpowiedniej akcji na /category, kliknięcie w nazwę kategorii obok posta umieszczonego na /task/accepted bądź /task/task (administrator), bądź przez  link /task/category/{id}

        📌 lista postów od najnowszego do najstarszego z paginacją po 10 rekordów na stronie,
            ✅ Dwie wersje jedna widoczna dla administratora z postami od użytkowników anonimowych /task/task, oraz lista publiczna z zaakceptowanymi ogłoszeniami /task/accepted

        📌 administrator (logowanie, zmiana hasła, zmiana danych administratora),
            ✅ Logowanie działa poprzez /login

    dobry           (0/6)
        ⭕ rejestracja i logowanie użytkowników,
        ⭕ zarządzanie kontem użytkownika (zmiana danych, zmiana hasła),
        ⭕ każdy użytkownik zarządza tylko swoimi ogłoszeniami (każda zmiana ogłoszenia wymaga aktywacji administratora),
        ⭕ zarządzanie kontami użytkowników przez administratora (zmiana hasła, zmiana danych),
        ⭕ CRUD dla tagów, tagowanie ogłoszeń,
        ⭕ filtrowanie listy ogłoszeń względem tagów,
    bardzo dobry    (0/7)
        ⭕ zmiana uprawnień użytkowników (zmiana na administratora, odbieranie uprawnień, brak możliwości odebrania uprawnień ostatniemu administratorowi),
        ⭕ blokowanie kont użytkowników,
        ⭕ tworzenie ogłoszeń w formacie Markdown,
        ⭕ dodawanie plików graficznych do ogłoszeń,
        ⭕ komentowanie profili użytkowników przez innych zalogowanych użytkowników (ocena wiarygodności ogłoszeniodawcy)
        ⭕ wyszukiwarka ogłoszeń,
        ⭕ dodatkowe kryteria wyszukiwania, np. lokalizacja,